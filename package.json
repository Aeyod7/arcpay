{
  "name": "arcpay-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*",
    "services/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@arcpay/sdk && npm run build --workspace=@arcpay/ui && npm run build --workspace=arcpay-dashboard",
    "dev": "npm run dev --workspaces --parallel"
  }
}
